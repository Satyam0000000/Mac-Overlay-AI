# Mac-Overlay-AI
A lightweight personal AI overlay for macOS that stays accessible above any application. It provides quick text and image assistance through global shortcuts, while securely storing API keys locally with Keychain-backed encryption. The app uses OpenAI’s cloud models, so it avoids running a large local model on your Mac.
